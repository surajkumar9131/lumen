# Server
PORT=3000
# Leave HOST unset (default 0.0.0.0) so the API is reachable from the app/emulator. Use 127.0.0.1 only if you want localhost-only.
# HOST=0.0.0.0
NODE_ENV=development

# Auth: set to true to skip Firebase token verification (local dev only).
# When true, mobile app can set EXPO_PUBLIC_DISABLE_AUTH=true to bypass login and use a dev user.
DISABLE_AUTH=true

# Firebase
# Local credentials (pick one):
#   A) Service account JSON (if org allows): Firebase Console → Service accounts → Generate new private key
#      Save and set FIREBASE_SERVICE_ACCOUNT_PATH to its path
#   B) gcloud (when key creation is restricted): run `gcloud auth application-default login`
# Cloud Run: leave unset; ADC uses default SA (Firebase Admin, Firestore Admin, Storage Admin)
FIREBASE_PROJECT_ID=lumen-app-b36bb
FIREBASE_STORAGE_BUCKET=lumen-app-b36bb.appspot.com
# Required for local dev (ADC/metadata not available). Optional on Cloud Run.
# Download: Firebase Console → Service accounts → Generate new private key.
FIREBASE_SERVICE_ACCOUNT_PATH=./service-account.json

# Vision & TTS use ADC automatically (same credentials as Firebase)

# Google Generative AI (Gemini)
GEMINI_API_KEY=AIzaSyDVI2CfEEBwZrIGSkuLO8OBfKxtTlldazw

# Pinecone
PINECONE_API_KEY=pcsk_5vRfcF_3aJVctpuT6p5BETE9QYF7spyGTg3ERwgMir27VLKXVkQS3t4nD7qfM8QCF7MV2Q
PINECONE_INDEX_NAME=lumen-snippets
PINECONE_ENVIRONMENT=
